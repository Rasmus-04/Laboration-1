<UserControl x:Class="Laboration_1.Vy.SpelVy.SpelVy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Laboration_1.Vy.SpelVy"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding AllaSpel}" Name="lvMembers" SelectionMode="Single" SelectedItem="{Binding SelectedMember}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Titel" DisplayMemberBinding="{Binding Titel}" Width="75"/>
                    <GridViewColumn Header="Max spelare" DisplayMemberBinding="{Binding MaxPlayers}" Width="75"/>
                    <GridViewColumn Header="Min spelare" DisplayMemberBinding="{Binding MinPlayers}" Width="75" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
